def target(nums, target):
    l = []
    for num in nums:
        m = target - num
        if m in nums:
            index2 = nums.index(num)
            nums[index2] = -1
            if m in nums:
                index1 = nums.index(m)
                if index1 != index2:
                    l.append([index2, index1])
    if len(l) > 0:
        print(min(l))
    else:
        print(None)
    
                

target([9], 9)
